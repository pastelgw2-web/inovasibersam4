
import React from 'react';
import { Project, User, Donation, VolunteerApplication, ProjectCategory } from '../types';

interface UserDashboardProps {
  user: User;
  projects: Project[];
  donations: Donation[];
  volunteerApps: VolunteerApplication[];
  onViewProject: (id: string) => void;
}

const UserDashboard: React.FC<UserDashboardProps> = ({ user, projects, donations, volunteerApps, onViewProject }) => {
  // Global Impact Dummy Data
  const globalImpactData = [
    {
      category: ProjectCategory.TECHNOLOGY,
      icon: '🚀',
      color: 'from-blue-500 to-indigo-600',
      bgColor: 'bg-blue-50',
      textColor: 'text-blue-600',
      metrics: [
        { label: 'Tech Innovations', value: '142' },
        { label: 'Patent Support', value: '28' },
        { label: 'Industry Partners', value: '15' }
      ],
      description: 'Accelerating industrial efficiency through IoT and AI integration.'
    },
    {
      category: ProjectCategory.EDUCATION,
      icon: '📚',
      color: 'from-amber-400 to-orange-500',
      bgColor: 'bg-amber-50',
      textColor: 'text-amber-600',
      metrics: [
        { label: 'Students Reached', value: '12.5K' },
        { label: 'Rural Schools', value: '45' },
        { label: 'Learning Modules', value: '890' }
      ],
      description: 'Bridging the literacy gap in remote areas with digital toolkits.'
    },
    {
      category: ProjectCategory.HEALTH,
      icon: '🏥',
      color: 'from-rose-400 to-red-600',
      bgColor: 'bg-rose-50',
      textColor: 'text-rose-600',
      metrics: [
        { label: 'Lives Impacted', value: '4.2K' },
        { label: 'Devices Deployed', value: '156' },
        { label: 'Health Volunteers', value: '82' }
      ],
      description: 'Deploying low-cost medical devices and telemedicine portals.'
    },
    {
      category: ProjectCategory.SOCIAL,
      icon: '🤝',
      color: 'from-purple-500 to-fuchsia-600',
      bgColor: 'bg-purple-50',
      textColor: 'text-purple-600',
      metrics: [
        { label: 'Communities', value: '34' },
        { label: 'Social Startups', value: '56' },
        { label: 'Empowered Groups', value: '12K' }
      ],
      description: 'Fostering inclusion and economic growth for underprivileged groups.'
    },
    {
      category: ProjectCategory.ENVIRONMENT,
      icon: '🌱',
      color: 'from-emerald-400 to-teal-600',
      bgColor: 'bg-emerald-50',
      textColor: 'text-emerald-600',
      metrics: [
        { label: 'Plastic Recovered', value: '1.8 Tons' },
        { label: 'Carbon Offset', value: '450 tCO2e' },
        { label: 'Trees Planted', value: '5.2K' }
      ],
      description: 'Developing sustainable solutions for waste and renewable energy.'
    }
  ];

  // User Specific calculations
  const myProjects = projects.filter(p => p.innovatorId === user.id);
  const myProjectIds = myProjects.map(p => p.id);
  const totalRaised = myProjects.reduce((sum, p) => sum + p.currentFunding, 0);
  const activeTeamsCount = myProjects.reduce((sum, p) => sum + p.volunteersCount, 0);
  const relatedDonations = donations.filter(d => myProjectIds.includes(d.projectId));
  const relatedApps = volunteerApps.filter(a => myProjectIds.includes(a.projectId));

  const activities = [
    ...relatedDonations.map(d => ({ 
      type: 'donation', 
      name: d.donorName, 
      desc: `donated Rp ${d.amount.toLocaleString()} on`, 
      target: myProjects.find(p => p.id === d.projectId)?.title || 'Project',
      time: 'Recently',
      id: d.id
    })),
    ...relatedApps.map(a => ({ 
      type: 'volunteer', 
      name: 'A Candidate',
      desc: `applied for ${a.skillName} on`, 
      target: myProjects.find(p => p.id === a.projectId)?.title || 'Project',
      time: 'New Application',
      id: a.id
    }))
  ].slice(0, 5);

  return (
    <div className="max-w-7xl mx-auto px-4 py-10 animate-in fade-in duration-700 space-y-20">
      
      {/* 1. SECTION: GLOBAL IMPACT SUMMARY */}
      <section>
        <div className="flex flex-col md:flex-row justify-between items-end gap-6 mb-12">
          <div className="max-w-2xl">
            <span className="text-emerald-600 font-black text-[10px] uppercase tracking-[0.3em] mb-3 block">Collective Growth</span>
            <h2 className="text-4xl md:text-5xl font-black text-slate-900 tracking-tight leading-none mb-4">
              Our Shared <span className="text-emerald-600">Impact</span>
            </h2>
            <p className="text-slate-500 font-medium text-lg leading-relaxed">
              Tracking the real-world results of thousands of collaborators, donors, and visionaries working together for global innovation.
            </p>
          </div>
          <div className="bg-white px-8 py-6 rounded-3xl border border-slate-100 shadow-sm flex items-center gap-8">
            <div>
              <p className="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">Total Impact Value</p>
              <p className="text-2xl font-black text-slate-900">Rp 12.8B+</p>
            </div>
            <div className="w-px h-10 bg-slate-100"></div>
            <div>
              <p className="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-1">Active Innovators</p>
              <p className="text-2xl font-black text-slate-900">1,240</p>
            </div>
          </div>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-6">
          {globalImpactData.map((impact, idx) => (
            <div key={idx} className="group bg-white rounded-[2rem] p-6 border border-slate-100 hover:shadow-2xl hover:shadow-emerald-500/5 transition-all duration-500 flex flex-col h-full hover:-translate-y-2">
              <div className={`w-14 h-14 rounded-2xl ${impact.bgColor} flex items-center justify-center text-2xl mb-6 shadow-inner transition-transform group-hover:rotate-12`}>
                {impact.icon}
              </div>
              <h3 className="text-lg font-black text-slate-900 mb-3 uppercase tracking-tight">{impact.category}</h3>
              <p className="text-slate-500 text-xs font-medium leading-relaxed mb-8 flex-1">
                {impact.description}
              </p>
              
              <div className="space-y-4 pt-6 border-t border-slate-50">
                {impact.metrics.map((m, midx) => (
                  <div key={midx} className="flex justify-between items-center">
                    <span className="text-[10px] font-black text-slate-400 uppercase tracking-widest">{m.label}</span>
                    <span className={`text-sm font-black ${impact.textColor}`}>{m.value}</span>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* 2. SECTION: PERSONAL DASHBOARD */}
      <section className="pt-12 border-t border-slate-100">
        <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 mb-12">
          <div>
            <span className="text-indigo-600 font-black text-[10px] uppercase tracking-[0.3em] mb-3 block">Personal Dashboard</span>
            <h2 className="text-4xl font-black text-slate-900 tracking-tight leading-none">Hello, <span className="text-indigo-600">{user.name.split(' ')[0]}</span></h2>
            <p className="text-slate-500 font-medium mt-3 text-lg">Manage your personal contributions and innovation projects.</p>
          </div>
          <button className="px-8 py-4 bg-indigo-600 text-white font-black rounded-2xl shadow-xl shadow-indigo-100 hover:bg-indigo-700 transition-all hover:-translate-y-1 uppercase tracking-widest text-[10px]">
            Generate My Impact Report
          </button>
        </div>

        {/* Stats Grid */}
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
          {[
            { label: 'Personal Funds', value: `Rp ${(totalRaised / 1000000).toFixed(1)}M`, icon: '💰', color: 'bg-indigo-50 text-indigo-600' },
            { label: 'My Teams', value: activeTeamsCount, icon: '👥', color: 'bg-emerald-50 text-emerald-600' },
            { label: 'Project Reach', value: '12.4K', icon: '📈', color: 'bg-amber-50 text-amber-600' },
            { label: 'Goal Status', value: '88%', icon: '❤️', color: 'bg-rose-50 text-rose-600' },
          ].map((stat, i) => (
            <div key={i} className="bg-white p-8 rounded-[2.5rem] border border-slate-100 shadow-sm flex items-center gap-6 transition-all hover:shadow-lg">
              <div className={`w-14 h-14 rounded-2xl flex items-center justify-center text-2xl ${stat.color} shadow-sm`}>
                {stat.icon}
              </div>
              <div>
                <p className="text-[10px] font-black uppercase text-slate-400 tracking-widest mb-1">{stat.label}</p>
                <p className="text-2xl font-black text-slate-900">{stat.value}</p>
              </div>
            </div>
          ))}
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Main: My Innovations Table */}
          <div className="lg:col-span-2 bg-white rounded-[2.5rem] border border-slate-100 shadow-sm overflow-hidden">
            <div className="p-8 border-b border-slate-50 flex justify-between items-center">
              <h2 className="text-xl font-black text-slate-800 uppercase tracking-tight">Active Proposals</h2>
              <button className="text-[10px] font-black text-indigo-600 hover:text-indigo-700 uppercase tracking-widest">View History</button>
            </div>
            
            <div className="overflow-x-auto">
              <table className="w-full text-left">
                <thead className="bg-slate-50/50">
                  <tr className="text-[10px] font-black uppercase text-slate-400 tracking-widest">
                    <th className="px-8 py-5">Innovation</th>
                    <th className="px-8 py-5 text-center">Status</th>
                    <th className="px-8 py-5">Funding</th>
                    <th className="px-8 py-5 text-center">Team</th>
                    <th className="px-8 py-5"></th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-slate-50">
                  {myProjects.length > 0 ? myProjects.map((p) => {
                    const percent = Math.min(100, Math.round((p.currentFunding / p.targetFunding) * 100));
                    return (
                      <tr key={p.id} className="hover:bg-slate-50/30 transition-colors">
                        <td className="px-8 py-6">
                          <div className="flex items-center gap-4">
                            <img src={p.imageUrl} className="w-12 h-12 rounded-xl object-cover shadow-sm border border-slate-100" alt="" />
                            <span className="font-black text-slate-800 text-sm tracking-tight">{p.title}</span>
                          </div>
                        </td>
                        <td className="px-8 py-6 text-center">
                          <span className="px-3 py-1 bg-emerald-100 text-emerald-700 text-[9px] font-black uppercase rounded-lg">
                            {p.status}
                          </span>
                        </td>
                        <td className="px-8 py-6">
                          <div className="w-32">
                            <div className="flex justify-between items-center mb-1.5">
                              <span className="text-[9px] font-black text-slate-400 uppercase">Rp {(p.currentFunding / 1000000).toFixed(1)}M</span>
                            </div>
                            <div className="h-1.5 w-full bg-slate-100 rounded-full overflow-hidden">
                              <div className="h-full bg-indigo-500 rounded-full" style={{ width: `${percent}%` }} />
                            </div>
                          </div>
                        </td>
                        <td className="px-8 py-6">
                          <div className="flex -space-x-2 justify-center">
                            {[1,2,3].map(i => (
                              <img key={i} src={`https://api.dicebear.com/7.x/avataaars/svg?seed=${i+p.id}`} className="w-8 h-8 rounded-full border-2 border-white bg-slate-100" alt="" />
                            ))}
                          </div>
                        </td>
                        <td className="px-8 py-6 text-center">
                          <button 
                            onClick={() => onViewProject(p.id)}
                            className="w-10 h-10 bg-slate-50 text-slate-400 hover:bg-indigo-600 hover:text-white rounded-xl transition-all flex items-center justify-center mx-auto"
                          >
                            <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                              <path fillRule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clipRule="evenodd" />
                            </svg>
                          </button>
                        </td>
                      </tr>
                    );
                  }) : (
                    <tr>
                      <td colSpan={5} className="px-8 py-20 text-center text-slate-400 font-black uppercase text-[10px] tracking-widest opacity-50">
                        No personal innovations started yet
                      </td>
                    </tr>
                  )}
                </tbody>
              </table>
            </div>
          </div>

          {/* Sidebar: Recent Collaborations */}
          <div className="bg-white rounded-[2.5rem] border border-slate-100 shadow-sm p-8 flex flex-col h-full">
            <h2 className="text-xl font-black text-slate-800 mb-8 uppercase tracking-tight">Activity Feed</h2>
            <div className="space-y-8 flex-1">
              {activities.length > 0 ? activities.map((act, i) => (
                <div key={i} className="flex gap-4 group cursor-default">
                  <div className="relative">
                    <img src={`https://api.dicebear.com/7.x/avataaars/svg?seed=${act.id}`} className="w-12 h-12 rounded-2xl bg-slate-50 border border-slate-100" alt="" />
                    <div className={`absolute -bottom-1 -right-1 w-5 h-5 rounded-full border-2 border-white flex items-center justify-center text-[8px] ${act.type === 'donation' ? 'bg-emerald-500' : 'bg-indigo-500'}`}>
                      {act.type === 'donation' ? '💰' : '🤝'}
                    </div>
                  </div>
                  <div className="flex-1">
                    <p className="text-sm leading-snug">
                      <span className="font-black text-slate-900">{act.name}</span>{' '}
                      <span className="text-slate-500">{act.desc}</span>{' '}
                      <span className="font-black text-indigo-600 tracking-tight">{act.target}</span>
                    </p>
                    <p className="text-[10px] font-black uppercase text-slate-400 mt-1.5 tracking-widest">{act.time}</p>
                  </div>
                </div>
              )) : (
                <div className="text-center py-20">
                  <div className="text-4xl mb-4 opacity-20">📡</div>
                  <p className="text-[10px] font-black text-slate-300 uppercase tracking-widest">Awaiting collaborations</p>
                </div>
              )}
            </div>
            <button className="w-full mt-10 py-5 bg-slate-50 text-slate-500 font-black rounded-2xl hover:bg-slate-100 transition-all text-[10px] uppercase tracking-widest">
              View Full History
            </button>
          </div>
        </div>
      </section>
    </div>
  );
};

export default UserDashboard;
